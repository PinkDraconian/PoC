name: TEST

on:
  pull_request:
    types:
      - labeled
      - synchronize
      - assigned

jobs:
  TEST:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: TEST
        run: echo "${{ github.event.pull_request.head.ref }}"
